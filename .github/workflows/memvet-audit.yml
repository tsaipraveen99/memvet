name: MemVet audit

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  memory-audit:
    name: Audit engineering memory
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull request
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MemVet
        run: python -m pip install .

      - name: Audit pull-request memory
        shell: bash
        run: |
          set +e
          memvet audit --base "origin/${{ github.base_ref }}" \
            | tee "$RUNNER_TEMP/memvet-audit.txt"
          audit_status=${PIPESTATUS[0]}
          set -e

          {
            echo "## MemVet audit"
            echo
            echo '```text'
            cat "$RUNNER_TEMP/memvet-audit.txt"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

          exit "$audit_status"

      - name: Publish audit comment
        if: always()
        continue-on-error: true
        uses: actions/github-script@v7
        env:
          AUDIT_REPORT_PATH: ${{ runner.temp }}/memvet-audit.txt
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const marker = '<!-- memvet-audit -->';
            const report = fs.readFileSync(process.env.AUDIT_REPORT_PATH, 'utf8').trim();
            const body = [
              marker,
              '## MemVet audit',
              '',
              '```text',
              report,
              '```',
              '',
              '_Updated automatically by MemVet._',
            ].join('\n');
            const comments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
              },
            );
            const existing = comments.find((comment) => comment.body?.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
